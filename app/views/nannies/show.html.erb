
      <div class="d-flex justify-content-center" >
        <div class="d-flex m-5">
          <div>
            <div class="block w-100">
              <% if @nanny.photo.attached? %>
              <%= cl_image_tag @nanny.photo.key, height: 300, width: 400, crop: :fill, class: "card-image" %>
              <% end %>
            </div>
            <div class='p-5'>
              <h2><%= @nanny.description %></h2>
              <p><strong>Rate: </strong><%=@nanny.price_per_hour%>€ per hour</p>
              <% if @nanny.certification %>
              <p><strong>Certified in: </strong><%= @nanny.certification %></p>
              <% end %>
              <p><strong>Age: </strong><%= @nanny.age %> years old</p>
              <p><strong>Location: </strong><%= @nanny.city %></p>
            <div><%= link_to "Book me", new_nanny_booking_path(@nanny), class: "btn btn-success" %></div>
            </div>
          </div>
            <div
              id="map"
              style="width: 600px;"
              data-markers="<%= @markers.to_json %>"
              data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
            ></div>
          </div>
      </div>





<p><%= link_to 'Back', nannies_path, class: "btn btn-secondary" %></p>
